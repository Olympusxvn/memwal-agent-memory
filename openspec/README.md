# OpenSpec proposals

Feature-level OpenSpec documents live in **`docs/specs/`** (e.g. `openspec-memwal-client.md`). This folder is reserved if we later split “proposal” vs “implemented spec” into a dedicated tree.

Until then, **do not duplicate** specs here — link from ADRs and `docs/ARCHITECTURE.md` to `docs/specs/` only.
