{
  "name": "memwalpp-cli",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "memwalpp": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@memwalpp/memwal-client": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
