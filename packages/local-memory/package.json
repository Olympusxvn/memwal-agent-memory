{
  "name": "@memwalpp/local-memory",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint yet\"",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "dependencies": {
    "@memwalpp/shared": "workspace:*",
    "better-sqlite3": "^11.8.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@vitest/ui": "^3.1.3",
    "typescript": "^5.7.2",
    "vitest": "^3.1.3"
  }
}
