{
  "name": "@memwalpp/memwal-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"no lint yet\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@memwalpp/shared": "workspace:*",
    "@mysten-incubation/memwal": "^0.0.7",
    "@mysten/sui": "^2.16.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.1.3"
  }
}
